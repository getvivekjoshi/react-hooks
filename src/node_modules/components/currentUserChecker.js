import { useEffect } from 'react'
import useFetch from 'hooks/useFetch'

const currentUserChecker = ({ childred }) => {
    const [{ response }, dofetch] = useFetch('/user')

    useEffect(() => {
        console.log('make only once');

    }, [])
    return children
}


export default currentUserChecker